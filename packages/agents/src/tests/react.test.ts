import { expect, describe, it } from "vitest";
import { useAgent } from "../react.ts";

// should be able to connect to an agent
// - defaults
// - with options

// connect with a websocket
// make a request

// sync state
// make rpc calls
